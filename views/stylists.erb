<h1>Stylists for Cutty McHaircuts Hair Salon</h1>

<% if !@all_stylists.any? %>
  <p>You have no stylists :(</p>
<% else %>
  <% @all_stylists.each() do |stylist| %>
    <p>Name: <a href="stylist/<%= stylist.id() %>"><%= stylist.name() %></a></p>
  <% end %>
<% end %>

<p><a href="/stylists/new">Add a new stylist to the system</a></p>
<p><a href="/">Home</a></p>
